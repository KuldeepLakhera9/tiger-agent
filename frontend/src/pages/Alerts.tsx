import { Bell } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';
export function Alerts() { return <PlaceholderPage eyebrow="Live signal feed" title="Alerts" description="Monitor model alerts and investigator requests in one focused queue. The demo currently has no additional alerts." icon={Bell} />; }