import { AlertTriangle, ArrowUpRight, Bot, CircleDot, Clock3, ExternalLink, Network, ShieldCheck } from 'lucide-react';
import { useParams } from 'react-router-dom';
import { HHG_002_DATA } from '../services/demoData';

function Panel({ title, icon: Icon, children, className = '' }: { title: string; icon: React.ElementType; children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-slate-800 bg-slate-900/65 ${className}`}><div className="flex items-center gap-2 border-b border-slate-800 px-5 py-4"><Icon size={16} className="text-cyan-300" /><h2 className="text-sm font-semibold text-slate-200">{title}</h2></div><div className="p-5">{children}</div></section>; }

export function InvestigationWorkspace() { const { id = 'HHG-002' } = useParams(); const data = id === 'HHG-002' ? HHG_002_DATA : HHG_002_DATA; const fields = [['Pattern', data.pattern], ['Card', data.card], ['Channel', data.channel], ['Exposure', `$${data.exposure.toFixed(2)}`]]; return <div className="mx-auto max-w-[1600px] space-y-5 p-5 sm:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs uppercase tracking-[0.18em] text-cyan-300">Investigation workspace</p><h1 className="mt-2 text-2xl font-semibold text-white">{data.id} <span className="text-slate-600">/</span> transaction review</h1></div><div className="flex items-center gap-2 text-xs text-slate-500"><Clock3 size={14} /> Last synced in demo mode</div></div><section className="grid grid-cols-2 gap-3 rounded-xl border border-slate-800 bg-slate-900/70 p-4 md:grid-cols-6">{[['Case ID', data.id], ['Status', data.status], ['Customer', data.customer], ['Transaction', data.transaction], ['Amount', `$${data.amount.toFixed(2)}`], ['Risk Score', data.risk.toFixed(2)]].map(([label, value]) => <div key={label} className="border-l border-slate-800 pl-4 first:border-0"><p className="text-[10px] uppercase tracking-[0.14em] text-slate-500">{label}</p><p className={`mt-2 text-sm font-semibold ${label === 'Risk Score' ? 'text-amber-300' : 'text-slate-100'}`}>{value}</p></div>)}</section><div className="grid grid-cols-12 gap-5"><Panel title="Case Summary" icon={ShieldCheck} className="col-span-12 lg:col-span-3"><div className="space-y-4">{fields.map(([label, value]) => <div key={label}><p className="text-xs text-slate-500">{label}</p><p className="mt-1 text-sm text-slate-200">{value}</p></div>)}<div className="rounded-lg border border-amber-400/20 bg-amber-400/5 p-3 text-xs leading-5 text-amber-200"><AlertTriangle size={15} className="mb-2" />Risk is a model input, not a verdict.</div></div></Panel><Panel title="Relationship Graph" icon={Network} className="col-span-12 lg:col-span-6"><div className="relative flex min-h-[260px] items-center justify-center overflow-hidden rounded-lg border border-dashed border-slate-700 bg-slate-950/70"><div className="absolute inset-0 grid-noise opacity-50" /><div className="relative text-center"><div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full border border-cyan-300/50 bg-cyan-400/10 text-cyan-300"><CircleDot size={25} /></div><p className="text-sm font-medium text-slate-300">Graph canvas reserved</p><p className="mx-auto mt-2 max-w-xs text-xs leading-5 text-slate-500">React Flow integration will render verified transaction relationships here.</p><span className="mt-4 inline-flex rounded-full border border-slate-700 px-2.5 py-1 text-[10px] uppercase tracking-[0.14em] text-slate-500">No fabricated relationships</span></div></div></Panel><Panel title="Investigation Copilot" icon={Bot} className="col-span-12 lg:col-span-3"><div className="flex items-center justify-between"><span className="rounded-full bg-violet-400/10 px-2 py-1 text-[10px] font-semibold uppercase tracking-[0.14em] text-violet-300">Demo data</span><span className="text-xs text-slate-600">Phase 1</span></div><div className="mt-5 space-y-4">{['Alert context retrieved', 'Card history queued', 'Evidence review pending'].map((step, index) => <div key={step} className="flex items-start gap-3"><span className={`mt-0.5 flex h-5 w-5 items-center justify-center rounded-full text-[10px] ${index < 2 ? 'bg-emerald-400/15 text-emerald-300' : 'bg-slate-800 text-slate-500'}`}>{index < 2 ? '✓' : index + 1}</span><span className="text-xs leading-5 text-slate-400">{step}</span></div>)}</div><div className="mt-6 rounded-lg border border-violet-400/20 bg-violet-400/5 p-3 text-xs leading-5 text-violet-200">Assessment will be produced by the investigation engine in a later phase.</div></Panel></div><div className="grid grid-cols-12 gap-5"><Panel title="Evidence Timeline" icon={Clock3} className="col-span-12 lg:col-span-9"><div className="space-y-4"><div className="flex gap-4 border-l border-cyan-400/40 pl-4"><div><p className="text-xs font-medium text-slate-200">Flagged transaction {data.transaction}</p><p className="mt-1 text-xs text-slate-500">Online purchase · ${data.amount.toFixed(2)} · Risk {data.risk.toFixed(2)}</p></div></div><div className="flex gap-4 border-l border-slate-700 pl-4"><div><p className="text-xs font-medium text-slate-200">Device Evidence: Unavailable</p><p className="mt-1 text-xs text-slate-500">No usable identity record is attached to this transaction. This is not shared-device evidence.</p></div></div></div></Panel><Panel title="Next Best Action" icon={ArrowUpRight} className="col-span-12 lg:col-span-3"><div className="rounded-lg border border-cyan-400/20 bg-cyan-400/5 p-4"><p className="text-sm font-semibold text-cyan-200">Step-up verification</p><p className="mt-2 text-xs leading-5 text-slate-400">Demo placeholder only. Final policy action is not connected to the backend yet.</p></div><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><ExternalLink size={13} /> Awaiting evidence engine</div></Panel></div></div>; }