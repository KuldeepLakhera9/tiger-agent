import { Network } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';
export function GraphExplorer() { return <PlaceholderPage eyebrow="Relationship intelligence" title="Graph Explorer" description="Explore verified links between transactions, cards, customers, devices, and historical cases once the graph service is connected." icon={Network} />; }